import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {COLORS} from '../../../util/Theme';

export const styles = StyleSheet.create({
  container: {padding: 10, alignSelf: 'center'},
  txt: {
    fontSize: 14,
    color: COLORS.black,
  },
});
